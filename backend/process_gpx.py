"""
TrailRun — process_gpx.py
Fase 2: Procesamiento de rutas GPX exportadas desde la PWA.

Uso:
    python process_gpx.py ruta.gpx

Salida:
    ruta_enriched.geojson   → Para la visualización 3D (Fase 3)
    ruta_stats.json         → Estadísticas completas de la sesión
    ruta_elevation.png      → Perfil de elevación con matplotlib

TODO (Fase 2):
    - Parsear GPX con gpxpy
    - Enriquecer puntos con elevación SRTM via rasterio
    - Calcular métricas derivadas (pace por segmento, VAM, etc.)
    - Exportar GeoJSON para CesiumJS / deck.gl
"""

# Este archivo se completará en la Fase 2 del proyecto.
# La PWA (app/) ya exporta archivos GPX estándar listos para procesar aquí.
